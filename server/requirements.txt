fastapi==0.115.0
uvicorn[standard]==0.30.6
numpy==2.0.2
python-multipart==0.0.12
python-dotenv==1.0.1

# ── DB integration / Database ────────────────────────────
# sqlalchemy==2.0.36

# ── Database ──────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
alembic==1.13.3             # DB migrations

# Local dev — SQLite async driver (zero setup, works out of the box)
aiosqlite==0.20.0

# Production — uncomment when switching to PostgreSQL
# asyncpg==0.30.0